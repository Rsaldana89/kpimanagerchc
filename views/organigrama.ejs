<%- include('partials/header') %>

<h2 class="mb-4">Organigrama</h2>
<p class="text-muted">Generado dinámicamente a partir de la relación de puestos (<em>responde a</em>). Si actualizas la tabla de puestos, el organigrama cambia automáticamente.</p>

<div class="org-toolbar mb-2">
  <div class="btn-group" role="group" aria-label="Controles de zoom">
    <button type="button" class="btn btn-outline-secondary btn-sm" id="zoomOut" title="Alejar">−</button>
    <input id="zoomRange" type="range" min="0.25" max="3.5" step="0.05" value="1" />
    <button type="button" class="btn btn-outline-secondary btn-sm" id="zoomIn" title="Acercar">+</button>
  </div>
  <div class="btn-group ms-2" role="group" aria-label="Controles de vista">
    <button type="button" class="btn btn-outline-secondary btn-sm" id="fitScreen" title="Ajustar a pantalla">Ajustar</button>
    <button type="button" class="btn btn-outline-secondary btn-sm" id="centerRoot" title="Centrar">Centrar</button>
    <button type="button" class="btn btn-outline-secondary btn-sm" id="expandAll" title="Expandir todo">Expandir</button>
    <button type="button" class="btn btn-outline-secondary btn-sm" id="collapseAll" title="Colapsar todo">Colapsar</button>
  </div>
  <div class="ms-auto text-muted small d-none d-md-block">
    Tip: arrastra para moverte • rueda del mouse para zoom • click en “−/＋” para colapsar ramas • color = departamento
  </div>
</div>

<div class="org-viewport" id="orgViewport">
  <div class="org-stage" id="orgStage" aria-label="Organigrama">
    <!-- Se inserta HTML del organigrama generado en el controlador -->
    <%- treeHtml %>
  </div>
</div>

<style>
/* Toolbar */
.org-toolbar{
  display:flex;
  gap: .5rem;
  align-items:center;
  flex-wrap:wrap;
}
.org-toolbar #zoomRange{
  width: 180px;
  accent-color: #6c757d;
}

/* Viewport (sin scroll, con pan/zoom) */
.org-viewport{
  position:relative;
  width: 100%;
  height: calc(100vh - 220px);
  min-height: 520px;
  border: 1px solid #e9ecef;
  border-radius: 12px;
  background: #f8f9fa;
  overflow:hidden;
}
.org-viewport.org-compact .org-card{ min-width: 140px; }
.org-stage{
  position:absolute;
  left:0;
  top:0;
  transform-origin: 0 0;
  will-change: transform;
  cursor: grab;
  padding: 20px;
}
.org-stage.dragging{ cursor: grabbing; }

/*
  Organigrama en árbol centrado (vertical):
  - El nodo raíz queda centrado arriba
  - Los hijos se reparten horizontalmente debajo
  - Conectores padre-hijo con pseudo-elementos
*/
.org-list,
.org-list ul{
  list-style:none;
  margin:0;
  padding:0;
  position:relative;
  display:flex;
  justify-content:center;
}

/* Permite que el árbol tome su ancho real y habilita scroll horizontal */
.org-list{ width:max-content; }

/* Evitar que el CSS global agregue márgenes que rompen conectores */
.org-list li{ margin:0; }

/* Separación vertical entre niveles */
.org-list ul{
  padding-top: 24px;
}

.org-list li{
  list-style-type:none;
  text-align:center;
  position:relative;
  padding: 24px 10px 0 10px;
}

/* Conectores horizontales entre hermanos (arriba del nodo) */
.org-list li::before,
.org-list li::after{
  content:'';
  position:absolute;
  top:0;
  right:50%;
  border-top:1px solid #cfcfcf;
  width:50%;
  height:24px;
}

/* Lado derecho del conector + bajada al nodo */
.org-list li::after{
  right:auto;
  left:50%;
  border-left:1px solid #cfcfcf;
  width:50%;
}

/* Si es hijo único, sin conectores */
.org-list li:only-child::after,
.org-list li:only-child::before{
  display:none;
}
.org-list li:only-child{
  padding-top: 0;
}

/* Ajustes para primer/último hijo (evitar línea extendida) */
.org-list li:first-child::before,
.org-list li:last-child::after{
  border:0 none;
}
.org-list li:last-child::before{
  border-right:1px solid #cfcfcf;
  border-radius:0 6px 0 0;
}
.org-list li:first-child::after{
  border-radius:6px 0 0 0;
}

/* Conector vertical del padre hacia la línea de hijos */
.org-list ul::before{
  content:'';
  position:absolute;
  top:0;
  left:50%;
  border-left:1px solid #cfcfcf;
  width:0;
  height:24px;
}
.org-card{
  background:#fff;
  border:1px solid #e6e6e6;
  border-left: 6px solid var(--dept-color, #adb5bd);
  border-radius:12px;
  padding:0.5rem 0.75rem;
  display:inline-block;
  box-shadow:0 1px 2px rgba(0,0,0,0.04);
  min-width: 220px;
  position: relative;
}

.org-card strong{ display:block; color:#243449; letter-spacing: .2px; }
.org-meta{
  margin-top: 2px;
  font-size: 0.78rem;
  color: #6c757d;
  text-transform: uppercase;
  letter-spacing: .6px;
}

.org-meta::before{
  content:'';
  display:inline-block;
  width: 9px;
  height: 9px;
  border-radius: 999px;
  background: var(--dept-color, #adb5bd);
  margin-right: 6px;
  transform: translateY(-1px);
}

/* Tamaños por nivel: raíz grande, reportes directos mediano, resto compacto */
.org-node[data-depth="0"] > .org-card{ min-width: 260px; padding: .65rem .85rem; }
.org-node[data-depth="0"] > .org-card strong{ font-size: 1.05rem; }

.org-node[data-depth="1"] > .org-card{ min-width: 190px; padding: .55rem .7rem; }
.org-node[data-depth="1"] > .org-card strong{ font-size: .95rem; }

.org-node[data-depth]:not([data-depth="0"]):not([data-depth="1"]) > .org-card{
  min-width: 170px;
  padding: .5rem .65rem;
}
.org-node[data-depth]:not([data-depth="0"]):not([data-depth="1"]) > .org-card strong{ font-size: .9rem; }

/* Botón colapsar/expandir */
.org-toggle{
  position:absolute;
  top: 6px;
  right: 6px;
  width: 26px;
  height: 26px;
  border-radius: 999px;
  border: 1px solid #dee2e6;
  background: #fff;
  color: #495057;
  font-weight: 700;
  line-height: 1;
  display:flex;
  align-items:center;
  justify-content:center;
  cursor:pointer;
}
.org-toggle:hover{ background:#f1f3f5; }

/* Colapsado: ocultar hijos */
.org-node.collapsed > ul{ display:none; }
</style>

<script>
(function(){
  const viewport = document.getElementById('orgViewport');
  const stage = document.getElementById('orgStage');
  const zoomRange = document.getElementById('zoomRange');
  const zoomIn = document.getElementById('zoomIn');
  const zoomOut = document.getElementById('zoomOut');
  const fitScreen = document.getElementById('fitScreen');
  const centerRoot = document.getElementById('centerRoot');
  const expandAll = document.getElementById('expandAll');
  const collapseAll = document.getElementById('collapseAll');

  let scale = 1;
  let tx = 0;
  let ty = 0;
  let isPanning = false;
  let startX = 0;
  let startY = 0;
  let userInteracted = false;

  function clamp(v, min, max){ return Math.max(min, Math.min(max, v)); }
  function applyTransform(){
    stage.style.transform = `translate(${tx}px, ${ty}px) scale(${scale})`;
    zoomRange.value = String(scale);
  }

  function getStageSize(){
    // Medir tamaño real del contenido visible (más confiable que getBoundingClientRect)
    const prev = stage.style.transform;
    stage.style.transform = 'none';
    // scrollWidth/scrollHeight respetan display:none de ramas colapsadas
    const w = stage.scrollWidth;
    const h = stage.scrollHeight;
    stage.style.transform = prev;
    return { w, h };
  }

  function fitToScreen(mode = 'fit'){
    const v = viewport.getBoundingClientRect();
    const r = getStageSize();
    const padding = 36;
    const availableW = Math.max(200, v.width - padding);
    const availableH = Math.max(200, v.height - padding);

    // Auto-compact si el organigrama es muy ancho
    if (r.w > availableW * 1.8) viewport.classList.add('org-compact');
    else viewport.classList.remove('org-compact');

    // Re-medimos por si cambió a compacto
    const r2 = getStageSize();
    const sFit = Math.min(availableW / r2.w, availableH / r2.h);
    let s = sFit;

    // Modo "focus": prioriza legibilidad (tamaño de tarjetas) aunque no quepa todo.
    // Útil cuando el 2º nivel tiene muchos puestos y el fit haría todo minúsculo.
    if (mode === 'focus') {
      const MIN_READABLE = 0.75;
      s = Math.max(sFit, MIN_READABLE);
    }

    scale = clamp(Number(s.toFixed(2)), 0.25, 3.5);

    // Centrar y dar margen arriba
    tx = (v.width - r2.w * scale) / 2;
    ty = 18;
    applyTransform();
  }

  function setToggleIconForNode(li){
    const btn = li.querySelector(':scope > .org-card > .org-toggle');
    if(!btn) return;
    btn.textContent = li.classList.contains('collapsed') ? '+' : '−';
  }

  function collapseBelowVisibleLevels(visibleLevels){
    // visibleLevels: 1=solo raíz, 2=raíz + reportes directos, 3=+ nietos, etc.
    const maxDepthVisible = Math.max(0, visibleLevels - 1);
    stage.querySelectorAll('.org-node').forEach(li => {
      const d = parseInt(li.getAttribute('data-depth') || '0', 10);
      const hasChildren = li.getAttribute('data-has-children') === '1';
      // Para ocultar niveles más profundos, colapsamos el ÚLTIMO nivel visible
      // (p.ej. si visibleLevels=2, colapsamos depth>=1 para esconder depth 2+).
      if (hasChildren && d >= maxDepthVisible) li.classList.add('collapsed');
      else li.classList.remove('collapsed');
      setToggleIconForNode(li);
    });
  }

  function centerOnRoot(){
    const root = stage.querySelector('.org-list > .org-node > .org-card');
    if(!root) return;
    const v = viewport.getBoundingClientRect();

    // Medir root sin transform
    const prev = stage.style.transform;
    stage.style.transform = 'none';
    const rootRect = root.getBoundingClientRect();
    const stageRect = stage.getBoundingClientRect();
    stage.style.transform = prev;

    const rootCenterX = (rootRect.left - stageRect.left) + (rootRect.width/2);
    tx = (v.width/2) - (rootCenterX * scale);
    ty = 20;
    applyTransform();
  }

  // Pan
  viewport.addEventListener('mousedown', (e) => {
    // Ignorar si clic en botón
    if(e.target && e.target.classList && e.target.classList.contains('org-toggle')) return;
    userInteracted = true;
    isPanning = true;
    stage.classList.add('dragging');
    startX = e.clientX - tx;
    startY = e.clientY - ty;
  });
  window.addEventListener('mousemove', (e) => {
    if(!isPanning) return;
    tx = e.clientX - startX;
    ty = e.clientY - startY;
    applyTransform();
  });
  window.addEventListener('mouseup', () => {
    isPanning = false;
    stage.classList.remove('dragging');
  });

  // Zoom (slider y botones)
  function setScale(newScale, anchor){
    userInteracted = true;
    const oldScale = scale;
    scale = clamp(newScale, 0.25, 3.5);
    if(anchor){
      // Zoom hacia el cursor (mantener punto bajo cursor)
      const v = viewport.getBoundingClientRect();
      const ax = anchor.clientX - v.left;
      const ay = anchor.clientY - v.top;
      tx = ax - (ax - tx) * (scale / oldScale);
      ty = ay - (ay - ty) * (scale / oldScale);
    }
    applyTransform();
  }
  zoomRange.addEventListener('input', () => setScale(parseFloat(zoomRange.value)));
  zoomIn.addEventListener('click', () => setScale(scale + 0.1));
  zoomOut.addEventListener('click', () => setScale(scale - 0.1));

  // Wheel zoom
  viewport.addEventListener('wheel', (e) => {
    e.preventDefault();
    const delta = e.deltaY > 0 ? -0.08 : 0.08;
    setScale(scale + delta, e);
  }, { passive: false });

  // Colapsar/expandir ramas
  stage.addEventListener('click', (e) => {
    const btn = e.target && e.target.closest ? e.target.closest('.org-toggle') : null;
    if(!btn) return;
    userInteracted = true;
    const li = btn.closest('.org-node');
    if(!li) return;
    li.classList.toggle('collapsed');
    btn.textContent = li.classList.contains('collapsed') ? '+' : '−';
    // Reajuste inteligente:
    // - Si colapsas, hacemos focus (más grande y legible)
    // - Si expandes, hacemos fit (ver más)
    requestAnimationFrame(() => {
      fitToScreen(li.classList.contains('collapsed') ? 'focus' : 'fit');
      centerOnRoot();
    });
  });

  // Expandir/colapsar todo
  expandAll.addEventListener('click', () => {
    userInteracted = true;
    stage.querySelectorAll('.org-node').forEach(n => n.classList.remove('collapsed'));
    stage.querySelectorAll('.org-toggle').forEach(b => b.textContent = '−');
    requestAnimationFrame(() => { fitToScreen('fit'); centerOnRoot(); });
  });
  collapseAll.addEventListener('click', () => {
    userInteracted = true;
    // Deja visible la raíz, colapsa el resto
    const roots = stage.querySelectorAll('.org-list > .org-node');
    stage.querySelectorAll('.org-node').forEach(n => n.classList.add('collapsed'));
    roots.forEach(r => r.classList.remove('collapsed'));
    stage.querySelectorAll('.org-toggle').forEach(b => {
      const li = b.closest('.org-node');
      b.textContent = li && li.classList.contains('collapsed') ? '+' : '−';
    });
    requestAnimationFrame(() => { fitToScreen('focus'); centerOnRoot(); });
  });

  // Ajustar y centrar
  fitScreen.addEventListener('click', () => fitToScreen('fit'));
  centerRoot.addEventListener('click', centerOnRoot);

  // Inicial: fit a pantalla
  function init(){
    // Inicial ultra intuitivo:
    // 1) Mostrar raíz + reportes directos
    // 2) Ocultar lo demás (colapsado)
    // 3) Ajustar para que se vea GRANDE y centrado
    requestAnimationFrame(() => {
      collapseBelowVisibleLevels(2);
      // Arranque: grande, claro y centrado (aunque requiera pan)
      fitToScreen('focus');
      centerOnRoot();
    });
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }

  // Reajustar si cambias tamaño de ventana
  window.addEventListener('resize', () => {
    // Solo auto-ajustar si el usuario no ha navegado (evita "brincos")
    if(userInteracted) return;
    requestAnimationFrame(() => fitToScreen());
  });
})();
</script>

<%- include('partials/footer') %>
